import Di{{properCase name}} from './src/{{name}}.vue'

Di{{properCase name}}.install = Vue => {
  Vue.component(Lg{{properCase name}}.name, Di{{properCase name}})
}

export default Di{{properCase name}}
